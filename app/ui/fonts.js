import { Geist, Open_Sans, Lora } from "next/font/google";

export const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const lora = Lora({
    subsets: ["latin"],
})

export const openSans = Open_Sans({
    subsets: ["latin"],
})